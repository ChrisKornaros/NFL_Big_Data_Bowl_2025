CREATE TABLE bronze.tracking_week_4(gameId BIGINT, playId BIGINT, nflId VARCHAR, displayName VARCHAR, frameId BIGINT, frameType VARCHAR, ""time"" TIMESTAMP, jerseyNumber VARCHAR, club VARCHAR, playDirection VARCHAR, x DOUBLE, y DOUBLE, s DOUBLE, a DOUBLE, dis DOUBLE, o VARCHAR, dir VARCHAR, ""event"" VARCHAR);
